# MIN_SCRIPTS — placeholder (not yet generated)

Run `uv run python code/sync_min_scripts.py` (or invoke the
sync-min-scripts skill) after the first round produces artifacts. This file
is generated-only: never hand-edit.
