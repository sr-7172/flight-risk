---
description: Have the author agent draft or update the LaTeX paper and/or beamer seminar deck from verified results. Usage: /write-up paper | slides | both [focus notes]. Run only when the latest round is closed and the validator is green.
---

Preconditions — check before delegating; if either fails, stop and report:
1. `python code/99_validate_outputs.py` exits 0.
2. REVIEW_REPORT.md's latest OVERALL section exists (a round has closed).

Delegate to the `author` subagent with this brief: read PROJECT.md, RESEARCH_LOG.md, REVIEW_REPORT.md, FINAL_RUN_LOG.md and the underlying output CSVs; then draft/update the target(s) in $ARGUMENTS (default: both). Tables and figures are generated by code from output CSVs (STANDING_RULES.md rule 13) — `code/spec_to_tex.py` handles standard spec CSVs. Compile with latexmk and fix errors. Results with open DEGENERATE-INFERENCE or MATCH-SENSITIVE flags stay out of the main text. If a needed table has no underlying CSV, append a WRITING-NEEDS entry to DECISIONS.md instead of improvising.

When the author returns, report: files written/updated, compile status, any WRITING-NEEDS entries, and any numbers it could not trace to a CSV (these are blockers, not footnotes).

$ARGUMENTS
